{% extends 'base.html.twig' %}

{% block title %}Evenements - Dooz Escape Game{% endblock %}
{% block title_og %}Evenements{% endblock %}

{% block body %}

    <!--Banner Single Escape-->
    <div class="single_banner">
        <div class="filter_carousel">
            <div class="filter_carousel_1"></div>
            <div class="filter_carousel_2"></div>
        </div>
        <img src="{{ asset('upload/travelogue/base/travelogue_banner.jpeg') }}" class="d-block w-100" alt="Image du carnet des voyageurs">
        <h2>{% trans %}Carnet des Voaygeurs{% endtrans%}</h2>
    </div>

    <div class="py-5">
        <h4 class="text-center">Le carnet des voyageurs revient très vite avec de nouvelles pages. A très vite !</h4>
    </div>


    {#    <div class="d-flex justify-content-center flex-wrap px-3 py-2">#}
    {#        {% for event in events %}#}
    {#            <div class="d-flex flex-column my-3">#}
    {#                <a href="">#}
    {#                    <div class="event_card">#}
    {#                        <h4 class="event_card_title">{{ event.name }}</h4>#}
    {#                        <img class="event_card_img" src="{{ asset('upload/events/2022/04/tournoi_pioneer.png') }}" alt="">#}
    {#                        <div class="d-flex flex-column align-items-center p-2">#}
    {#                            {{ event.beginDate|date("H") }}H{{ event.beginDate|date("i") }}-{{ event.endDate|date("H") }}H{{ event.endDate|date("H") }} | {{ event.beginDate|date('d/m/Y') }}#}
    {#                            <p class="event_card_location m-0">MALLEUS MALLEFICARUM</p>#}
    {#                        </div>#}
    {#                    </div>#}
    {#                </a>#}
    {#            </div>#}
    {#        {% endfor %}#}
    {#    </div>#}




{% endblock %}
